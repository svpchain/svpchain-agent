package brand

// AppDisplayName is the user-visible product name (Finder, Dock, window title, About).
const AppDisplayName = "SVPChain Agent"

// BundleID is the macOS bundle identifier and the OS config-directory name
// (prefs, memory, run logs, skills, in-app update staging).
const BundleID = "com.svpchain.agent"

// AppBundleName is the macOS .app bundle directory name.
const AppBundleName = AppDisplayName + ".app"

// AppFolderName is the Windows release folder name inside the zip.
const AppFolderName = AppDisplayName
